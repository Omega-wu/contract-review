**审核点**：{review}
**你的任务**：基于以上审核点，判断表格内容是否合格。

# 严格指令
1.  **输出必须纯净**：你的响应有且仅有一个合法的JSON对象。JSON 格式必须完全正确：
- 所有键和字符串值必须用英文双引号包围
- 键值对之间用英文逗号分隔
- **最后一个键值对后面不能有逗号**
- 严禁在JSON对象之外添加任何解释、推理、道歉、总结或问候语。
2.  **结果必须二选一**：“审核结果”的值只能是字符串 `"合格"` 或 `"不合格"`，不得使用其他表述。

# 输出格式
你必须且只能输出如下格式的JSON：

```json
{
"审核结果": "合格",
"审核依据": "此处必须直接引用或高度概括审核点中的具体规则"
}